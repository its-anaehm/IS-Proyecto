
/**
 * Interface que simula un objerto de tipo Usuario
 */
export default interface User{
    firstName?: string,
    lastName?: string,
    email: string,
    role?: string,
    phone?: string,
    password: string
}